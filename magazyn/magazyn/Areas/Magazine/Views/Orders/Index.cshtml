@model IEnumerable<magazyn.Areas.Magazine.Models.Items_has_OrderList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Dodaj zamówienie", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Items)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_OrderList)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_Employees)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Items)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_OrderList)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Employees)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.Id }) |
            @Html.ActionLink("Kasuj", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
